define(function (require,module) {
  //"use strict";

  // Alias the module for easier identification.
  var sapp = module.exports = {};

  // The root path to run the application through.
  sapp.root = "/";
});